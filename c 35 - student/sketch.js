var canvas;
var gameState = 0;
var playerCount = 0;
var database;

var form,player,game;

function setup(){
  canvas = createCanvas(400,400);


}

function draw(){

  background("lightblue");

  text(mouseX+","+mouseY,mouseX,mouseY)
}